const firstName= "dave"
const secondName = "fulton"

function data(firstName, secondName){
  return firstName.charAt().toUpperCase()   + " " + secondName.slice(0);
}

console.log(data(firstName, secondName))
